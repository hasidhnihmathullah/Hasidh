<?php
$servername = "localhost";
$username = "root";
$password = "";
$dbname = "velvet_vogue";
$port = 3307; // Matches your XAMPP port

$conn = new mysqli($servername, $username, $password, $dbname, $port);

if ($conn->connect_error) {
    die("Connection failed: " . $conn->connect_error);
}
?>